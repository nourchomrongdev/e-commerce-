"use client";

import Link from "next/link";
import { useState } from "react";
import PublicIcon from "@/components/icons/PublicIcon";
import { routes } from "@/lib/routeController";

const content = { purchases: ["Purchases", "Review your marketplace orders and purchase history.", ["#ORD-3658", "#ORD-3657", "#ORD-3656"]], downloads: ["Downloads", "Access your purchased files and download history.", ["Premium Template Pack", "UI Design Kit", "Icon Set Collection"]], licenses: ["Licenses", "Manage your active product licenses and keys.", ["Premium Template Pack", "Laravel API Mastery", "WordPress Plugin"]], reviews: ["My Reviews", "Share feedback on the products you have purchased.", ["Premium Template Pack", "UI Design Kit", "Icon Set Collection"]], account: ["Account Settings", "Manage your buyer profile and preferences.", ["Profile information", "Security and password", "Notification preferences"]] } as const;
type BuyerView = keyof typeof content;

export default function BuyerSection({ view }: { view: BuyerView }) {
  const [notice, setNotice] = useState(false);
  const [heading, description, records] = content[view];
  return <div className="mx-auto w-full max-w-[1400px]"><header className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><h1 className="text-2xl font-bold tracking-tight text-heading sm:text-[28px]">{heading}</h1><p className="mt-1 text-sm text-muted">{description}</p></div><button type="button" onClick={() => setNotice(true)} className="inline-flex items-center justify-center gap-2 rounded-lg bg-primary px-3 py-2 text-xs font-semibold text-white hover:bg-primary-hover"><PublicIcon name={view === "account" ? "edit" : "download"} className="h-3.5 w-3.5" />{view === "account" ? "Edit profile" : "Export"}</button></header>{notice && <p className="mt-4 rounded-lg bg-emerald-50 px-3 py-2 text-xs text-status-success">Action completed successfully.</p>}{view === "account" && <div className="mt-5 rounded-2xl border border-[#f0c6a5] bg-gradient-to-r from-orange-50 via-white to-orange-50 p-5 shadow-sm"><div className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div className="flex items-start gap-3"><span className="grid h-11 w-11 place-items-center rounded-xl bg-white text-primary shadow-sm"><PublicIcon name="store" className="h-5 w-5" /></span><div><p className="text-[10px] font-semibold uppercase tracking-[0.12em] text-primary">Creator Program</p><h2 className="mt-1 text-base font-bold text-[#111b40]">Turn your buyer account into a creator storefront</h2><p className="mt-1 text-xs text-[#66718e]">Sell digital products, launch storefronts, and grow your audience.</p></div></div><Link href={routes.buyer.creatorProgram()} className="inline-flex items-center justify-center rounded-lg bg-primary px-4 py-2.5 text-xs font-semibold text-white no-underline hover:bg-primary-hover">Apply now</Link></div></div>}{view === "account" && <div className="mt-5 grid gap-4 md:grid-cols-3"><div className="rounded-2xl border border-border bg-white p-4 shadow-sm"><p className="text-[10px] font-semibold uppercase tracking-[0.12em] text-muted-soft">Ready to sell</p><p className="mt-2 text-xl font-bold text-[#111b40]">Monetize faster</p><p className="mt-1 text-xs text-[#66718e]">Launch your marketplace storefront and manage products in hours.</p></div><div className="rounded-2xl border border-border bg-white p-4 shadow-sm"><p className="text-[10px] font-semibold uppercase tracking-[0.12em] text-muted-soft">Trust</p><p className="mt-2 text-xl font-bold text-[#111b40]">Verified badge</p><p className="mt-1 text-xs text-[#66718e]">Build credibility and increase conversion with creator verification.</p></div><div className="rounded-2xl border border-border bg-white p-4 shadow-sm"><p className="text-[10px] font-semibold uppercase tracking-[0.12em] text-muted-soft">Support</p><p className="mt-2 text-xl font-bold text-[#111b40]">Creator tools</p><p className="mt-1 text-xs text-[#66718e]">Access payouts, analytics, license tools, and storefront management.</p></div></div>}<section className="mt-6 overflow-hidden rounded-2xl border border-border bg-white shadow-sm"><div className="border-b border-divider px-5 py-4"><h2 className="text-sm font-bold text-heading">{heading}</h2></div><div className="divide-y divide-divider">{records.map((record, index) => <article key={record} className="flex flex-col gap-3 px-5 py-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-lg bg-orange-50 text-primary"><PublicIcon name={view === "licenses" ? "key" : view === "downloads" ? "download" : view === "account" ? "user" : view === "reviews" ? "verified" : "receipt"} className="h-4 w-4" /></span><div><p className="text-xs font-semibold text-body">{record}</p><p className="mt-1 text-[10px] text-muted">{view === "account" ? "Update your account details" : index === 0 ? "Updated recently" : "May {22 - index}, 2025"}</p></div></div><span className={`w-fit rounded-full px-2.5 py-1 text-[9px] font-semibold ${view === "licenses" ? "bg-emerald-50 text-status-success" : "bg-surface-muted text-muted"}`}>{view === "licenses" ? "Active" : view === "account" ? "Manage" : "View details"}</span></article>)}</div></section></div>;
}
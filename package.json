{
  "name": "digital_products_downloads_marketplace",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend -- --port 3000\" \"npm run dev --prefix frontend_admin -- --port 3001\" \"npm run dev --prefix backend_service\"",
    "migrate": "npm run migrate --prefix backend_service"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
